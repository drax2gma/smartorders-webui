<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Üzenet küldése - SmartOrders</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>
<body class="bg-light">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-body">
                        <h2 class="card-title text-center mb-4">Üzenet küldése az adminnak</h2>
                        <form hx-post="/message" hx-target="#result">
                            <div class="mb-3">
                                <label for="message_type" class="form-label">Üzenet típusa</label>
                                <select class="form-select" id="message_type" name="message_type" required>
                                    <option value="">Válassz...</option>
                                    <option value="password_reset">Jelszó visszaállítás kérése</option>
                                    <option value="order_cancel">Rendelés törlése</option>
                                    <option value="support">Általános segítségkérés</option>
                                </select>
                            </div>
                            <button type="submit" class="btn btn-primary w-100">Üzenet küldése</button>
                        </form>
                        <div id="result" class="mt-3"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
