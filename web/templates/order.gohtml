<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rendelés - SmartOrders</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="mb-4">Rendelés leadása</h1>
        <div class="row">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-body">
                        <form hx-post="/order" hx-target="#result">
                            <div class="mb-3">
                                <label for="product" class="form-label">Válassz terméket</label>
                                <select class="form-select" id="product" name="product_id" required>
                                    <option value="">Válassz...</option>
                                    {{range .}}
                                    <option value="{{.ID}}">{{.Name}} - {{.Price}} Ft</option>
                                    {{end}}
                                </select>
                            </div>
                            <button type="submit" class="btn btn-primary">Rendelés leadása</button>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div id="result" class="mt-3"></div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
