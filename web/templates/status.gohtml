<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rendelés státusza - SmartOrders</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="mb-4">Rendelések státusza</h1>
        <div class="card shadow">
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Rendelés ID</th>
                            <th>Termék</th>
                            <th>Ár</th>
                            <th>Státusz</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{range .}}
                        <tr>
                            <td>{{.Order.ID}}</td>
                            <td>{{.Product.Name}}</td>
                            <td>{{.Order.TotalPrice}} Ft</td>
                            <td>
                                <span class="badge bg-{{if eq .Order.Status "pending"}}warning
                                    {{else if eq .Order.Status "processing"}}info
                                    {{else if eq .Order.Status "shipped"}}primary
                                    {{else if eq .Order.Status "delivered"}}success
                                    {{else}}secondary{{end}}">
                                    {{.Order.Status}}
                                </span>
                            </td>
                        </tr>
                        {{end}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
