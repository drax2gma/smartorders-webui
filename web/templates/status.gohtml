<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rendelés státusza - SmartOrders</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="mb-4">Rendelések státusza</h1>
        <div class="card shadow">
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Rendelés ID</th>
                            <th>Termék</th>
                            <th>Státusz</th>
                            <th>Összeg</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{range .}}
                        <tr>
                            <td>{{.ID}}</td>
                            <td>{{range .Products}}{{.Name}}{{end}}</td>
                            <td>
                                <span class="badge bg-{{if eq .Status "pending"}}warning
                                    {{else if eq .Status "processing"}}info
                                    {{else if eq .Status "shipped"}}primary
                                    {{else if eq .Status "delivered"}}success
                                    {{else}}secondary{{end}}">
                                    {{.Status}}
                                </span>
                            </td>
                            <td>{{.TotalPrice}} Ft</td>
                        </tr>
                        {{end}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
